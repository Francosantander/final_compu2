Clonar el repositorio

En caso de no tener instalado docker y no poder ejecutar el docker container:

- Situarse dentro de la carpeta /src 

- Ejecutar el comando $pip3 install -r requirements.txt

- Ejecutar el comando $/bin/python3 server.py -p <"puerto a utilizar">

En caso de tener instalado docker y poder ejecutar dockers containers

- Ejecutar $docker build -t serveraiohttp .
- Ejecutar $docker run -v <"Directorio donde almacenar los kml">:/opt/app/files/ -p 5000:5000 serveraiohttp